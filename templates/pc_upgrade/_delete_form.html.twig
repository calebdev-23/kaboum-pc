<form method="post" action="{{ path('app_pc_upgrade_delete', {'id': pc_upgrade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pc_upgrade.id) }}">
    <button class="btn">Supprimer</button>
</form>
